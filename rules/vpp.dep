
SPATH       := $($(LIBVPP)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/libvpp.mk rules/libvpp.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(LIBVPP)_CACHE_MODE  := GIT_CONTENT_SHA 
$(LIBVPP)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(LIBVPP)_DEP_FILES   := $(DEP_FILES)

